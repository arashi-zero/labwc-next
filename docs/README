Config layout for ~/.config/labwc-next/
- autostart
- environment
- shutdown
- xinitrc
- *.toml       (any .toml file — config, theme colors, keybinds, menus, etc.)

See config.toml for a fully documented reference with all default values.

